# 对战工厂租借池设计对齐说明（基于 pokeemerald-expansion）

## 1. 参考项目中的核心设计

参考实现位于：
- `reference/pokeemerald-expansion/src/battle_factory.c`
- `reference/pokeemerald-expansion/src/data/battle_frontier/battle_frontier_mons.h`

核心规则：
1. **同源池**  
   租借池与对手池都从 `gBattleFrontierMons` 里抽取（同一套预设集合）。
2. **挑战档位区间**  
   通过 `GetFactoryMonId(lvlMode, challengeNum, useBetterRange)` 从“当前挑战档位区间”选宝可梦。
3. **租借历史加成（仅作用于租借池）**  
   `GetNumPastRentalsRank` 根据历史租借次数得到 `rentalRank`（0~5），并使租借池前 `rentalRank` 个名额使用 `useBetterRange = TRUE` 的更高档位区间。
4. **去重约束**  
   租借池/对手池都要做：种族不重复、道具不重复；对手池还要避免与当前租借候选池撞种族。

## 2. 当前项目改造目标

由于当前项目不是固定 `FrontierMon` 编号池，而是在线生成 `GamePokemon`，采用“语义等价”的方式对齐：

1. **同源生成器**  
   租借池与对手池继续复用同一个 `buildFactoryPool`。
2. **挑战档位质量带**  
   以“质量偏置（quality bias）”模拟参考项目中的“区间档位”，按挑战序号（challenge num）取基础档位。
3. **更高档位（better range）**  
   对租借池前 `rentalRank` 个名额使用“升一档”的质量偏置，对齐参考项目 `useBetterRange` 行为。
4. **对手保持同档抽取**  
   对手池使用当前挑战档位基础偏置，不吃租借历史升档逻辑；额外保持项目已有 Boss 加强逻辑。

## 3. 对齐后的规则摘要（本项目）

1. `challengeNum = floor((stage - 1) / battlesPerSet)`，并做上限裁剪。
2. 租借池 6 只：
   - 每个名额独立决定 `qualityBias`；
   - 若名额序号 `< rentalRank`，使用 `betterRange`（升一档）。
3. 对手 3 只：
   - 使用当前挑战档位基础 `qualityBias`；
   - 仍保持：不与租借池/玩家队伍撞种族、队内不重复种族与道具。

## 4. 兼容与后续建议

1. 目前仍保留了项目自定义 Boss 强化（IV/等级）与提示文案机制。
2. 后续若要更接近原作，可引入固定“前沿配置池”（离线 Frontier sets），替代在线随机生成。
3. 若要加入“Open Level”分支，可直接扩展 `qualityBand` 表，不改主流程。

