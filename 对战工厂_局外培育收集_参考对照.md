# 对战工厂主流程 + 局外培育收集 参考对照文档

> 项目路径：`D:\Games\pokeFactory2`  
> 参考仓库：`D:\Games\pokeFactory2\reference\pokeemerald-expansion`  
> 版本日期：2026-04-03

## 1. 目标定义（本项目）

- 主流程：以对战工厂为核心，采用租借队伍、连胜推进、7 场一组节奏。
- 局外循环：加入培育与收集系统，作为长期成长内容。
- 经济循环：主流程产出代币，代币用于局外成长道具和系统解锁。

## 2. 当前项目能力快照

### 2.1 已有能力

- 已有工厂式流程骨架：`FACTORY_SELECT -> BATTLE -> FACTORY_SWAP`。
- 已有队伍租借基础：开局 6 选 3。
- 已有回合战斗和状态框架：命中、暴击、异常、能力阶段、天气伤害框架。
- 已有奖励/商店框架（偏 Roguelite）：奖励卡、重抽、商店项。

### 2.2 关键差距（相对“还原工厂 + 局外成长”目标）

- 主流程奖励结算不统一：胜利后直接进入换怪，`REWARD` 未成为固定主循环节点。
- 代币经济未闭环：存在消耗逻辑，但缺乏稳定主流程产出与局外持久化。
- 工厂规则混杂：捕捉、战内商店等机制与经典工厂模式冲突。
- 局外系统缺失：尚无培育中心、图鉴收集、局外商店与成长树。

## 3. 参考机制对照（pokeemerald-expansion）

| 设计主题 | 参考机制 | 参考文件 | 对本项目的落地建议 |
|---|---|---|---|
| 工厂节奏 | 7 场一组、设施常量、连胜记录 | `include/constants/battle_frontier.h` | 固化 `7 战一组`，显示`组内胜场/总连胜` |
| 工厂队伍生成 | 租借池生成、对手队生成、交换逻辑、质量控制 | `src/battle_factory.c` | 建立“租借池质量分层”与“交换次数统计” |
| 连胜与 BP 奖励 | 连胜统计、按设施/模式发放 BP、首领奖励加成 | `src/frontier_util.c` | 将“BP”映射为本项目代币，按阶段和里程碑发放 |
| 工厂战斗入口 | Factory 设施入口与队伍填充 | `src/battle_frontier.c` | 把当前模式切成 `Classic Factory` 主入口 |
| 代币消费 | Exchange Corner 兑换与扣点 | `src/data/battle_frontier/battle_frontier_exchange_corner.h` `data/maps/BattleFrontier_ExchangeServiceCorner/scripts.inc` `src/field_specials.c` | 建立局外代币商店，先做小型道具池 |
| 培育系统 | 相容度、产蛋、继承（性格/IV/特性/球种/蛋招） | `include/constants/daycare.h` `include/daycare.h` `src/daycare.c` `src/egg_hatch.c` | 先做“轻量培育 v1”：相容度+产蛋+基础继承 |
| 收集系统 | 图鉴 Seen/Caught 记录与统计 | `include/pokedex.h` `include/constants/pokedex.h` `src/pokedex.c` | 建立局外图鉴页，记录遭遇/持有/进化解锁 |

## 4. 功能增量池（可加入内容）

### P0（优先，先打通核心体验）

- 经典工厂主流程：`租借 -> 3v3 -> 赛后换怪 -> 下一场`，每 7 场结算一次。
- 代币结算：按胜场、组别、是否 Boss 战发放；失败时保底少量代币。
- 局外代币商店 v1：成长道具（例如：开局额外刷新券、初始租借池品质+、战后恢复加成）。
- 运行外循环：主流程结束后回到“基地界面”而非直接重开。

### P1（中期，构建长期留存）

- 培育中心 v1：托管两只宝可梦，按局外时间/挑战次数推进产蛋。
- 孵化系统 v1：孵化后进入收藏池（不直接破坏工厂公平性，可用作解锁/皮肤/词条来源）。
- 图鉴与收藏册：Seen/Caught/Owned 三层记录，解锁代币奖励。

### P2（后期，强化深度和可玩性）

- 工厂双模式：`Classic`（贴近原作）+ `Rogue`（保留随机奖励和商店）。
- 每日种子挑战：同日同池，便于分享成绩。
- 元成长树：代币购买“赛外被动”，但确保不破坏对战工厂策略核心。

## 5. 具体实施计划（建议 4 个里程碑）

### M1：主流程收敛（3-5 天）

- 目标：把“工厂战斗循环 + 代币结算”跑通。
- 交付：
  - 固化 7 战节奏与组别结算页。
  - 胜利与失败都进入统一结算节点。
  - 代币产出规则配置化（JSON/TS 常量）。
- 验收：能连续完成 2 个组别，代币可稳定积累。

### M2：局外商店与成长道具（4-6 天）

- 目标：形成“打工厂 -> 拿代币 -> 买成长”的闭环。
- 交付：
  - 局外基地页与代币商店页。
  - 成长道具系统（被动加成、一次性消耗、解锁项）。
  - 存档持久化（本地存档版本号 + 迁移）。
- 验收：新存档和旧存档都能正确读取并消费代币。

### M3：培育与收集 v1（5-8 天）

- 目标：加入长期目标，不破坏主流程公平性。
- 交付：
  - 培育中心（相容度、产蛋进度、孵化）。
  - 图鉴页面（Seen/Caught/Owned 统计）。
  - 收藏奖励（图鉴节点奖励代币或外观）。
- 验收：完成 1 条简化“培育 -> 孵化 -> 收藏登记”链路。

### M4：平衡与扩展（持续迭代）

- 目标：强化可重复游玩与策略深度。
- 交付：
  - 工厂池质量分层与 AI 选招优化。
  - Classic/Rogue 双模式与每日挑战。
  - 数据看板（胜率、常用队、道具购买热度）。
- 验收：连续多局体验稳定，无明显滚雪球失衡。

## 6. 建议的数据结构草案

- `player_meta`
  - `tokens`: 代币余额
  - `factory_rank`: 工厂段位/组别历史
  - `unlock_flags`: 已解锁局外功能
- `run_state`
  - `stage`: 当前场次
  - `streak`: 当前连胜
  - `rental_team`: 当前租借队
  - `swap_count`: 本组交换次数
- `breeding_state`
  - `slots`: 培育槽
  - `egg_queue`: 蛋队列
  - `hatch_progress`: 孵化进度
- `collection_state`
  - `seen`: 已见
  - `caught`: 已收录
  - `owned_forms`: 已持有形态

## 7. 参考代码索引（便于后续逐文件对照）

- 工厂与前沿：
  - `reference/pokeemerald-expansion/include/constants/battle_frontier.h`
  - `reference/pokeemerald-expansion/include/constants/battle_factory.h`
  - `reference/pokeemerald-expansion/src/battle_factory.c`
  - `reference/pokeemerald-expansion/src/frontier_util.c`
  - `reference/pokeemerald-expansion/src/battle_frontier.c`
- 代币与兑换：
  - `reference/pokeemerald-expansion/src/data/battle_frontier/battle_frontier_exchange_corner.h`
  - `reference/pokeemerald-expansion/data/maps/BattleFrontier_ExchangeServiceCorner/scripts.inc`
  - `reference/pokeemerald-expansion/src/field_specials.c`
- 培育与孵蛋：
  - `reference/pokeemerald-expansion/include/constants/daycare.h`
  - `reference/pokeemerald-expansion/include/daycare.h`
  - `reference/pokeemerald-expansion/src/daycare.c`
  - `reference/pokeemerald-expansion/src/egg_hatch.c`
- 收集与图鉴：
  - `reference/pokeemerald-expansion/include/pokedex.h`
  - `reference/pokeemerald-expansion/include/constants/pokedex.h`
  - `reference/pokeemerald-expansion/src/pokedex.c`

## 8. 当前项目启动方式（清理后）

- 安装依赖：`npm install`
- 开发启动：`npm run dev`
- 类型检查：`npm run lint`
- 打包：`npm run build`

> 说明：本项目 `package.json` 的 `clean` 脚本为 `rm -rf dist`，在 Windows PowerShell 下可能不可用。若需清理构建产物，建议使用：`Remove-Item -Recurse -Force dist`。

## 9. 代币系统补充参考（新增）

以下是对“币体系”可继续参考的关键点，适合直接映射到当前项目：

- 奖励采用“分设施/分模式/分组别阶梯表”，而非单一公式  
  - 参考：`reference/pokeemerald-expansion/src/frontier_util.c` 的 `sBattlePointAwards`
  - 含义：同样是 7 战一组，但不同模式可以拥有不同增长曲线（前期平滑，后期封顶）。

- 组别结算索引有上限钳制，避免数值无限膨胀  
  - 参考：`GiveBattlePoints()` 中 `challengeNum` 越界后钳制到最后一档（30 档）。
  - 落地建议：你的配置表也保留“超出取最后档”。

- Boss 战采用固定额外奖励（+10 BP）  
  - 参考：`GiveBattlePoints()` 对 `TRAINER_FRONTIER_BRAIN` 追加奖励。
  - 落地建议：保留“第 7 战额外币”是合理方向，可继续作为稀缺币来源。

- 代币余额有全局硬上限（9999）与加减保护  
  - 参考：`MAX_BATTLE_FRONTIER_POINTS`、`GiveFrontierBattlePoints()`、`TakeFrontierBattlePoints()`
  - 落地建议：局外币建议加上限（例如 9999 或 99999）并保留统一增减 API。

- 代币发放与“展示统计”分离为两套字段  
  - 参考：`battlePoints`（可消费）与 `cardBattlePoints`（名片统计）并行存在。
  - 落地建议：你可以保留 `tokens`（可花）+ `lifetimeTokens`（生涯累计）两条线。

- 代币可被纳入日统计，支持活动任务/每日目标  
  - 参考：`IncrementDailyBattlePoints()`（`VAR_DAILY_BP`）。
  - 落地建议：可加“今日获得代币”任务，驱动每日活跃但不破坏长期平衡。

- 兑换商店采用“分品类柜台 + 明确定价 + 空间校验 + 成功后即时刷新余额”  
  - 参考：`BattleFrontier_ExchangeServiceCorner/scripts.inc`、`field_specials.c`
  - 细节：
    - 维他命价格非常低（1 BP）作为低门槛消耗。
    - 战斗向持有物集中在 48/64 BP。
    - 装饰物形成中高价梯度（16 ~ 256 BP）。
  - 落地建议：你的局外商店也可分为“常买低价 / 中价战力 / 高价收藏”三层。

- 工厂结果页会同时展示“连胜 + Rental Swap（交换/租借行为）”  
  - 参考：`ShowFactoryResultsWindow()`。
  - 落地建议：币结算可加入“过程行为修正”（如少换怪奖励、冒险奖励），形成玩法激励。

- 连胜活跃状态和中断恢复有独立标记  
  - 参考：`FACTORY_DATA_WIN_STREAK_ACTIVE`、`challengePaused`。
  - 落地建议：你的 Run 存档可以明确区分“进行中 run / 已结算 run”，避免刷币漏洞。

- 商店侧有“余额不足、背包/仓库满、确认购买、购买后反馈”完整流程  
  - 参考：`BattleFrontier_ExchangeServiceCorner/scripts.inc` 各分支。
  - 落地建议：局外商城尽量保留同等完整的失败反馈与回退路径。

## 10. 对战机制可参考清单（新增）

以下聚焦“对战本身与工厂战斗节奏”，优先级按推荐落地顺序排列：

- P0：工厂对手队生成的合法性约束（最该先补）
  - 参考：`reference/pokeemerald-expansion/src/battle_factory.c` `GenerateOpponentMons()`
  - 机制点：
    - 对手队内不重复种族；
    - 对手队内不重复持有物；
    - 对手队避免与本轮可租借池重叠；
    - 禁用特殊异常池（如 Unown）。
  - 你当前现状：`useFactoryFlow` 直接随机 ID 生成 3 只，缺少这些约束。

- P0：初始租借池质量分层（与挑战进度绑定）
  - 参考：`GenerateInitialRentalMons()` + `GetNumPastRentalsRank()`
  - 机制点：
    - 挑战越深，候选池上移；
    - 玩家历史租借/交换行为会影响初始租借质量；
    - 初始租借同样做“同种/同道具”去重。
  - 你当前现状：初始 6 选 3 是纯随机，长期体验波动较大。

- P0：Boss 局（第 7 战）战力抬升规则
  - 参考：`FillFactoryFrontierTrainerParty()`，最后一战使用更高 IV。
  - 机制点：
    - 不是单纯数值翻倍，而是“构筑质量 + AI + IV”协同抬升。
  - 你当前现状：第 7 战用统一比例增强（HP/属性），可进一步精细化。

- P1：AI 难度随组别进阶
  - 参考：`GetAiScriptsInBattleFactory()`
  - 机制点：
    - 前期弱 AI；
    - 中期启用基础坏招规避；
    - 后期启用击杀倾向与可行性检查；
    - Boss 使用完整 AI 标志。
  - 你当前现状：敌方主要随机出招，可升级为“分档策略 AI”。

- P1：结果页展示“连胜 + 交换次数”
  - 参考：`ShowFactoryResultsWindow()`（`Rental Swap`）
  - 机制点：
    - 玩家能明确看到“我靠多少次交换赢下来的”。
  - 落地建议：你可以把交换次数并入赛后结算和成就统计。

- P1：工厂专属规则收敛（减少混模）
  - 参考：官方 Factory 主循环是租借/对战/交换，不含工厂内捕捉。
  - 你当前现状：战斗中仍支持球捕捉逻辑，会稀释工厂策略核心。
  - 落地建议：工厂战内禁捕捉，收集来源改到局外培育/图鉴系统。

- P2：对手风格与队伍倾向信息系统
  - 参考：`GetOpponentMostCommonMonType()`、`GetOpponentBattleStyle()`
  - 机制点：
    - 通过“最常见属性/风格”给玩家战前策略信息，而不是纯盲打。
  - 落地建议：可做成简化提示条（如“对手偏高速特攻”）。

- P2：挑战状态机与中断恢复
  - 参考：`challengeStatus`、`challengePaused`、`WIN_STREAK_ACTIVE`
  - 机制点：
    - run 进行中与已结算严格分离，减少异常退出导致的状态漏洞。
  - 落地建议：你的本地存档可加 `runStatus` 字段与恢复校验。
