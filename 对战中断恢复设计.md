# 对战中断恢复设计

## 目标

为工厂对战增加“中断恢复”能力，保证用户刷新页面后仍能回到当前这场对战，而不是丢回首页或整场 run 重开。

本设计优先保证：

1. 回到同一场 battle，而不是只回到同一轮次。
2. 不保存“半个回合”的脏状态。
3. 使用当前 `localStorage` 同步存档能力即可落地。
4. 优先覆盖 `Battle Factory` 主战斗，事件战可后续扩展。

## 当前现状

当前存档只保存了粗粒度进度：

- `progress.totalRents / highestStreak / specialModeUnlocked`
- `factory.challengeStatus / curChallengeBattleNum / challengePaused / trainerIdsBySet`
- `events / collection / settings`

对应代码：

- `src/services/saveManager.ts`
- `src/features/game/hooks/usePokeFactoryGame.ts`

当前没有保存这些“正在打的战斗态”：

- `playerTeam / enemyTeam / enemy`
- `currentEnemyTrainer`
- `inventory`
- `turn / battleMenuTab`
- `battleLog`
- `activeBuffs / enemyBuffs`
- `weather / weatherTurns`
- `battleSpecialUsage`
- 敌方已使用过的特殊战斗资源状态

此外，战斗逻辑里有大量 `Math.random()`，并且消息/动画/切换是异步推进的：

- `src/features/game/hooks/useBattleController.ts`

这意味着如果在“消息播放中”或“敌方决策中”直接落盘，刷新后很容易恢复到一个逻辑不完整的中间态。

## 设计原则

### 1. 只保存稳定检查点

不要在以下阶段写 battle 快照：

- `isMessageProcessing === true`
- `loading === true`
- `isTransitioning === true`
- `playerAnim !== 'idle'`
- `enemyAnim !== 'idle'`
- `activeMoveType !== null`
- `isCatching === true`
- `showReplaceUI !== null`

只在“已经结算完成、等待下一次输入或下一次系统动作”的稳定点保存。

### 2. 表现层状态默认重置

以下状态恢复时不需要原样落盘，统一回默认值即可：

- `playerAnim = 'idle'`
- `enemyAnim = 'idle'`
- `activeMoveType = null`
- `isMessageProcessing = false`
- `isTransitioning = false`
- `loading = false`
- `catchSuccess = null`

### 3. 可序列化优先

`Item` 对象带有 `effect` 函数，不能直接作为 JSON 持久化，因此战斗快照里不要保存完整 `inventory: Item[]`，而是保存：

- `inventoryItemIds: string[]`

恢复时再通过物品注册表重建。

## 新增存档结构

建议把存档 schema 升到 `v5`，在 `factory` 下新增 `battleResume`：

```ts
battleResume: {
  status: 'EMPTY' | 'READY';
  battleKind: 'FACTORY';
  checkpointAt: string;
  checkpointSeq: number;

  stage: number;
  streak: number;
  swapCount: number;
  coins: number;
  totalRents: number;

  enemyAiTier: FactoryAiTier;
  specialModeUnlocked: boolean;
  specialBossBattleActive: boolean;
  battleSpecialUsage: BattleSpecialUsageState;
  enemySpecialUsage: BattleSpecialUsageState;

  turn: 'PLAYER' | 'ENEMY';
  battleMenuTab: 'MAIN' | 'MOVES' | 'POKEMON' | 'BAG';
  weather: 'none' | 'sunny' | 'rainy' | 'sandstorm' | 'hail';
  weatherTurns: number;
  activeBuffs: { atk: boolean; def: boolean };
  enemyBuffs: { atk: boolean; def: boolean };

  factoryRentals: GamePokemon[];
  selectedRentalIndices: number[];
  playerTeam: GamePokemon[];
  enemyTeam: GamePokemon[];
  currentEnemyTrainerId: string | null;
  inventoryItemIds: string[];
  battleLog: string[];
}
```

## 恢复时需要的附加约束

### 1. 训练家不直接存整个对象

`currentEnemyTrainer` 建议存 `id`：

- `currentEnemyTrainerId: string | null`

恢复时通过模板表查回对象。

这样更稳，也更容易做 schema 兼容。

### 2. `enemy` 不单独存

当前逻辑里敌方首发固定是 `enemyTeam[0]`，所以恢复时直接：

```ts
enemy = enemyTeam[0] ?? null
```

避免重复源。

### 3. run 元数据继续沿用现有字段

以下字段仍保留在现有 save 根结构：

- `selectedGens`
- `startLevel`
- `trainerIdsBySet`
- `currentLanguage`
- `collection`
- `events`

Battle resume 只补“当前 battle 快照”。

## 保存时机设计

### A. 自动保存主策略

每当 battle 进入稳定检查点时，生成一份 battle snapshot 并覆盖保存。

推荐的稳定点：

1. `spawnEnemy()` 完成，进入战斗后。
2. 玩家换人完成后。
3. 使用道具完成后。
4. 一次完整 `executeTurn()` 结束后。
5. `sendOutNextEnemy()` / `sendOutNextPlayer()` 完成后。
6. 敌方切人完成后。

### B. 页面刷新兜底

额外监听：

- `visibilitychange`
- `beforeunload`

处理原则：

1. 若当前有“最近一次稳定检查点”，直接把它同步写入 `localStorage`。
2. 不尝试在 `beforeunload` 里等待异步消息或补完整个半回合。

也就是说，刷新时保证回到“最近一个稳定 battle 状态”。

## 恢复流程

应用启动时：

1. `loadSaveData()` 读出 `factory.battleResume`
2. 若 `status !== 'READY'`，按普通启动流程走
3. 若 `status === 'READY'`，进行 battle hydrate

hydrate 时恢复这些状态：

- `stage / streak / swapCount / coins / totalRents`
- `specialModeUnlocked / specialBossBattleActive`
- `battleSpecialUsage / enemySpecialUsage`
- `enemyAiTier`
- `factoryRentals / selectedRentalIndices`
- `playerTeam / enemyTeam`
- `currentEnemyTrainer`
- `inventory`
- `battleLog`
- `turn / battleMenuTab`
- `weather / weatherTurns`
- `activeBuffs / enemyBuffs`
- `gameState = 'BATTLE'`

同时重置这些表现层状态：

- `loading = false`
- `isTransitioning = false`
- `isMessageProcessing = false`
- `playerAnim = 'idle'`
- `enemyAnim = 'idle'`
- `activeMoveType = null`
- `catchSuccess = null`

恢复完成后可追加一条系统日志：

`已恢复到第 {stage} 战的当前对战。`

## 清理时机

以下情况必须清空 `battleResume`：

1. Battle 进入 `ROUND_RESULT`
2. Battle 离开到 `FACTORY_SWAP`
3. run 结束回到首页
4. 玩家主动开始新 run
5. 导入存档时 battle 快照与当前 schema 不兼容

对应原则：

- “有 battle 才有 battleResume”
- 离开 battle 立即清空 battleResume

## 关键实现点

### 1. 提供 battle snapshot 序列化器

建议新增：

- `serializeBattleSnapshot(...)`
- `hydrateBattleSnapshot(...)`
- `clearBattleSnapshot(...)`

建议放在：

- `src/services/saveManager.ts`
  或
- `src/features/game/utils/battleResume.ts`

### 2. 敌方特殊资源状态要从 ref 提升为可持久化状态

当前敌方特殊技使用状态在：

- `useBattleController.ts` 的 `enemySpecialUsageRef`

这个状态如果不外露，就无法恢复。

建议改成：

1. `useBattleController` 接收 `enemySpecialUsage`
2. 同时接收 `setEnemySpecialUsage`

这样 battle resume 可以完整保存。

### 3. 物品恢复要走注册表

需要一个稳定的 `itemId -> Item` 查表：

```ts
const ITEM_BY_ID = Object.fromEntries(ALL_ITEMS.map((item) => [item.id, item]));
```

恢复时：

```ts
inventory = inventoryItemIds.map((id) => ITEM_BY_ID[id]).filter(Boolean)
```

### 4. 训练家恢复要走 id 查表

建议新增：

```ts
getFactoryTrainerTemplateById(id: string): FactoryTrainerTemplate | null
```

## 推荐落地顺序

### Phase 1：可用版

目标：刷新后回到当前 battle 的最近稳定点。

实现：

1. 新增 `battleResume` schema
2. 保存/恢复 battle 核心状态
3. 清空/恢复逻辑接入 `usePokeFactoryGame`
4. 不处理精确“消息播放到一半”的恢复，只恢复稳定点

### Phase 2：精确版

目标：进一步减少刷新造成的回退感。

可选扩展：

1. 增加 `checkpointSeq`
2. 记录更精确的 battle phase
3. 统一 RNG 入口，替换 battle 内 `Math.random()`
4. 持久化 RNG 状态，做到敌方后续决策也完全一致

## 建议边界

本次建议先只覆盖：

- `gameState === 'BATTLE'`
- `eventBattleActive === false`

事件战包含捕捉和额外分支，建议单独做二期扩展。

## 结论

最稳的方案不是“任何时刻都硬保存”，而是：

1. 在 battle 稳定检查点保存完整快照
2. 刷新时恢复到最近一个稳定 battle 状态
3. 离开 battle 即清空 battle 快照

这样实现复杂度可控，而且能真正保证“刷新页面还能回到当前对战”。
