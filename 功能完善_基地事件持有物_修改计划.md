# 功能完善修改计划：基地系统、事件派遣 UI、持有物机制

> 更新时间：2026-04-10  
> 适用项目：`D:\Games\pokeFactory2`

## 零、进度回填（2026-04-10）

> 本次回填包含：代码现状核对 + 本轮继续完善后的状态。

总体完成度（估算）：约 **88%**

- 模块 1（`BASE` 独立化）：约 **70%**（进行中）
  - 已完成：`BOOT -> BASE`、`enterBase -> BASE`、局外页面返回 `BASE`、`BASE + currentBaseTab` 已驱动 `HOME/COLLECTION/EVENTS` 页面分发、`BaseScreen` 入口文件已抽出并接管 `BASE/HOME`。
  - 未完成：`BaseScreen` 内部内容与 `StartScreen` 进一步解耦、`START` 职责继续收敛、`SHOP/BREEDING/PROFILE` 同层结构补齐。
- 模块 2（事件派遣手选 UI）：约 **88%**（进行中）
  - 已完成：手选弹层、搜索、匹配排序、清除、推荐、状态可视化。
  - 本轮新增：发起派遣时不再隐式自动挑选；未选择/不匹配会给出明确提示并阻断派遣开始。
  - 未完成：可选的组件拆分（`DispatchPokemonPicker` / `DispatchRegionCard`）与进一步复用整理。
- 模块 3（持有物战斗化）：约 **96%**（进行中）
  - 已完成（参考清单全量）：参考集出现的 40 种持有物均已接入战斗流程（含 `choice_band / focus_band / white_herb / mental_herb / leppa_berry / thick_club / leek / deep_sea_scale`）。
  - 未完成：配置层独立化（`factoryHeldItems.ts` / `heldItemResolution.ts`）与专项平衡/回归测试收尾。

### 本轮（2026-04-10）新增落地

1. `scope_lens`：暴击判定接入（提升暴击阶段）。
2. `bright_powder`：命中判定接入（降低攻击方最终命中）。
3. `kings_rock`：命中后畏缩钩子接入（额外畏缩概率）。
4. `leftovers`：回合结束恢复钩子接入（按最大 HP 比例恢复）。
5. `quick_claw`：回合前先手钩子接入（在玩家出招阶段加入先手拦截流程与判定）。
6. `shell_bell`：造成伤害后回复钩子接入。
7. 状态果接入：`lum / cheri / chesto / pecha / rawst / persim`（状态触发后解状态并消耗）。
8. `sitrus_berry`：半血阈值恢复并消耗（受击与回合结算链路均可触发）。
9. `lax_incense` + 类型增伤道具接入（`charcoal/mystic-water/magnet/black-belt/poison-barb/never-melt-ice/twisted-spoon/hard-stone/sharp-beak/silver-powder/metal-coat/miracle-seed/soft-sand/black-glasses/silk-scarf`）。
10. `liechi/petaya/salac` 接入（低血线能力提升并消耗）。
11. 剩余清单接入：`choice_band/focus_band/white_herb/mental_herb/leppa_berry/thick_club/leek/deep_sea_scale`。

## 一、目标

将目前“已经有雏形但还未闭环”的 3 个方向，转换为可直接实施的修改计划：

1. 基地系统从“复用 `START` 的主页”升级为独立的 `BASE` 流程
2. 事件派遣补齐“手动选择派遣宝可梦”的 UI 和交互闭环
3. 工厂持有物从“伪持有物池”升级为真实战斗机制的一部分

## 二、总体实施顺序

建议按下面顺序推进：

1. 先拆出独立 `BASE`
- 这是所有局外系统的入口，先理顺状态机，后续商店、培育、事件都更容易接入

2. 再补事件派遣选择 UI
- 当前状态层已经齐全，UI 缺口清晰，改造成本相对可控

3. 最后做持有物战斗机制
- 这是战斗规则级改动，影响面最大，适合在局外流程稳定后推进

## 三、模块 1：基地系统独立为 `BASE`

### 1. 当前问题

- 当前主页实际仍挂在 `START`
- `currentBaseTab` 已存在，但没有真正驱动统一的基地容器
- 多个页面返回时直接 `setGameState('START')`
- `BASE` 已在 `GameState` 中预留，但 `GameView` 没有真正渲染它

### 2. 修改目标

- `BASE` 成为正式的局外主状态
- `START` 只保留给启动/引导阶段，或者在第一阶段仅作为兼容过渡态
- 基地首页、图鉴、事件、设置等页面统一从 `BASE` 进入并返回 `BASE`
- `currentBaseTab` 真正承担基地内导航状态

### 3. 涉及文件

- `src/types.ts`
- `src/features/game/components/GameView.tsx`
- `src/features/game/components/game-view/StartScreen.tsx`
- `src/features/game/components/game-view/CollectionScreen.tsx`
- `src/features/game/components/game-view/EventsScreen.tsx`
- `src/features/game/components/game-view/SettingsScreen.tsx`
- `src/features/game/components/game-view/BootLoadingScreen.tsx`
- `src/features/game/hooks/usePokeFactoryGame.ts`
- `src/features/game/view-model/actions.ts`
- `src/features/game/view-model/state.ts`

### 4. 具体改动计划

#### 阶段 A：状态机落地

1. 让 `BOOT -> BASE`
- 当前 `BootLoadingScreen.tsx` 结束后进入 `START`
- 改为进入 `BASE`

2. 让 `GameView` 正式支持 `BASE`
- 在 `GameView.tsx` 中新增 `gameState === 'BASE'` 的渲染分支
- 第一阶段可直接复用当前 `StartScreen`

3. 调整 `enterBase`
- 当前 `enterBase()` 会 `setGameState('START')`
- 改为 `setGameState('BASE')`
- 同时统一 `setCurrentBaseTab('HOME')`

4. 调整局外返回路径
- `CollectionScreen.tsx`
- `EventsScreen.tsx`
- `SettingsScreen.tsx`

这些页面的“返回主页”统一改为返回 `BASE`

#### 阶段 B：基地内导航统一

1. 统一“基地入口”和“基地子页”的概念
- `BASE + currentBaseTab='HOME'` 表示基地首页
- `BASE + currentBaseTab='COLLECTION'` 表示图鉴入口
- `BASE + currentBaseTab='EVENTS'` 表示事件入口
- 后续可扩展 `SHOP`、`BREEDING`

2. 让 `openBaseTab()` 成为基地内主导航 API
- 从主页点图鉴/事件时，先设置 `currentBaseTab`
- 再进入对应页面

3. 整理 `START` 的职责
- 第一阶段：保留 `START` 但不再作为常规主页使用
- 第二阶段：若无额外引导需求，可逐步将 `START` 的职责并入 `BASE`

#### 阶段 C：UI 结构拆分

1. 抽出 `BaseScreen`
- 从当前 `StartScreen` 演化出真正的 `BaseScreen`
- `StartScreen` 保留为过渡文件或直接被 `BaseScreen` 取代

2. 将当前主页内容定义为 `Base Home`
- 工厂入口
- 运行摘要
- 底部 dock
- 事件/图鉴/后续商店和培育入口

3. 为后续 `SHOP` / `BREEDING` 预留同层级结构
- 避免后续继续走“单页跳转拼接”路线

### 5. 验收标准

- 启动后进入 `BASE` 而不是 `START`
- 图鉴、事件、设置页返回时都回到 `BASE`
- `enterBase()`、回合结算、失败结算都统一回到 `BASE`
- `currentBaseTab` 真正用于基地内导航，而不是只有状态字段

## 四、模块 2：事件派遣增加手动选择派遣宝可梦 UI

### 1. 当前问题

- 状态层已经有 `eventDispatchPokemonByRegion`
- 也有 `setEventDispatchPokemon(regionId, pokemonId)`
- 但 `EventsScreen.tsx` 没有提供手动选择入口
- 当前更偏向“自动选一只符合条件的宝可梦”

### 2. 修改目标

- 每个区域都能明确选择派遣的宝可梦
- 玩家能看到已选对象、类型匹配状态和更换入口
- 保留“自动挑选”作为辅助功能，而不是唯一入口

### 3. 涉及文件

- `src/features/game/components/game-view/EventsScreen.tsx`
- `src/features/game/hooks/usePokeFactoryGame.ts`
- `src/features/game/view-model/state.ts`
- `src/features/game/view-model/actions.ts`
- 可选新增：
  - `src/features/game/components/game-view/events/DispatchPokemonPicker.tsx`
  - `src/features/game/components/game-view/events/DispatchRegionCard.tsx`

### 4. 具体改动计划

#### 阶段 A：先补最小可用交互

1. 在区域卡片中增加“当前派遣宝可梦”展示
- 显示名称
- 显示图标/头像
- 显示属性
- 显示是否满足该地区类型要求

2. 增加“选择宝可梦”入口
- 建议做成弹层或底部抽屉，不要直接塞满主卡片
- 数据源优先使用 `collectionOwnedIds`

3. 增加“清除选择”和“自动推荐”
- `清除选择`：将该区域设为 `null`
- `自动推荐`：复用现有 `autoPickDispatchPokemon()`

#### 阶段 B：候选列表与筛选

1. 构建派遣候选列表
- 来源：`collectionOwnedIds`
- 每项显示：
  - 宝可梦名
  - 编号
  - 图标
  - 属性
  - 是否匹配地区要求

2. 列表排序建议
- 先显示匹配 requiredTypes 的宝可梦
- 再按编号或名称排序
- 后续可增加“最近使用”“推荐度”等排序

3. 候选信息获取策略
- 第一阶段可直接用 `fetchPokemon`
- 若性能不够，再补缓存层或基于 `pokedexClient` 的轻量快照读取

#### 阶段 C：派遣反馈补全

1. 在区域卡片中显示当前选择状态
- 未选择
- 已选择但属性不匹配
- 已选择且可派遣

2. 发起派遣时不再默认静默自动选
- 若未选择，优先提示“请选择派遣宝可梦”
- 保留一个显式的“自动挑选”按钮

3. 结果与选择联动
- 领取结果后保留该区域当前选中宝可梦，方便连续派遣
- 若宝可梦不再可用，再在下一轮派遣时做兜底校验

### 5. 验收标准

- 每个地区都能手动指定派遣宝可梦
- 可视化看到是否匹配该地区要求
- 能清除选择、自动推荐、重新选择
- 未选择时不会再完全依赖隐式自动挑选

## 五、模块 3：持有物机制从展示字段升级为真实战斗规则

### 1. 当前问题

- `factoryHeldItemId` 已经进入工厂生成数据
- 但目前主要用于展示和特殊战斗触发判断
- `factoryBattle.ts` 中的 `heldItemPool` 仍然是“伪持有物池”
- 真正战斗结算没有系统性读取这些持有物效果

### 2. 修改目标

- 工厂持有物不再只是标签，而是真正影响战斗
- 先支持当前池中的持有物，再决定是否扩展
- 持有物机制和现有战斗系统保持兼容，不一次性重写整个结算层

### 3. 当前池内物品范围

当前 `FACTORY_BATTLE_CONFIG.heldItemPool` 包含：

- `leftovers`
- `choice_band`
- `scope_lens`
- `white_herb`
- `quick_claw`
- `bright_powder`
- `focus_band`
- `mental_herb`
- `kings_rock`

### 4. 建议的实施策略

分 3 批落地，不建议一次全部打通。

#### 第一批：无状态或低状态成本物品

优先实现：

- `leftovers`
- `scope_lens`
- `quick_claw`
- `bright_powder`
- `kings_rock`

原因：

- 这些物品最容易嵌入现有流程
- 对状态持久化要求较低
- 对战斗体验提升明显

#### 第二批：一次性触发类物品

再实现：

- `white_herb`
- `focus_band`
- `mental_herb`

原因：

- 这些道具需要“是否已消耗”的运行时状态
- 要考虑对战中断恢复时如何保存

#### 第三批：行为约束类物品

最后实现：

- `choice_band`

原因：

- 官方语义不仅是伤害加成，还有“锁招”
- 这是当前系统里改动面最大的一个持有物

### 5. 涉及文件

- `src/features/game/config/factoryBattle.ts`
- `src/features/game/hooks/useBattleController.ts`
- `src/features/game/lib/battleResolution.ts`
- `src/types.ts`
- `src/services/saveManager.ts`
- 可选新增：
  - `src/features/game/config/factoryHeldItems.ts`
  - `src/features/game/lib/heldItemResolution.ts`

### 6. 具体改动计划

#### 阶段 A：建立持有物机制配置层

1. 新增工厂持有物配置表
- 建议单独抽出 `factoryHeldItems.ts`
- 为每个 `heldItemId` 定义：
  - 显示名
  - 触发时机
  - 效果类型
  - 是否一次性消耗

2. 不再把 `heldItemPool` 只当字符串池
- `factoryBattle.ts` 保留“工厂抽样池”
- 实际效果逻辑由独立配置表解释

#### 阶段 B：在战斗流程中插入持有物钩子

建议按战斗时机拆钩子：

1. 回合前
- `quick_claw`

2. 命中判定前
- `bright_powder`

3. 伤害/暴击判定时
- `scope_lens`
- `choice_band`

4. 命中后附加效果判定时
- `kings_rock`

5. 回合结束时
- `leftovers`

6. 异常/能力变化触发时
- `white_herb`
- `mental_herb`
- `focus_band`

#### 阶段 C：补运行时状态

当前 `GamePokemon` 只有 `factoryHeldItemId`，建议增加运行时字段，例如：

- `factoryHeldItemConsumed?: boolean`
- `factoryChoiceLockedMoveName?: string | null`

如果不想把状态压进 `GamePokemon`，也可以做一层 battle runtime state，但第一版更建议直接挂在 `GamePokemon` 上，改动更小。

#### 阶段 D：兼容中断恢复

由于项目已经支持 `battleResume`，持有物状态必须进入恢复体系：

- 一次性道具是否已触发
- `choice_band` 是否已锁招
- 若未来有更多战斗内临时状态，也应统一纳入恢复快照

### 7. 每个物品的第一版建议语义

#### `leftovers`

- 每回合结束回复最大 HP 的固定比例

#### `scope_lens`

- 提高暴击率

#### `quick_claw`

- 回合开始有概率抢先行动

#### `bright_powder`

- 降低对手命中率

#### `kings_rock`

- 攻击命中后追加低概率畏缩

#### `white_herb`

- 当能力下降时自动恢复一次并消耗

#### `focus_band`

- 低概率以 1 HP 留场并消耗或按设计不消耗

#### `mental_herb`

- 被关键限制状态影响时解除一次并消耗

#### `choice_band`

- 提高物攻输出
- 第一版就建议做“锁招”，不要只做纯加伤，否则体验会和预期差太多

### 8. 验收标准

- 工厂持有物会实际影响战斗，不再只是展示字段
- 至少第一批 5 个物品能稳定生效
- 一次性持有物有清晰的消耗状态
- 对战中断恢复后，持有物状态不丢失

## 六、推荐里程碑

### M1：BASE 独立化（进行中，约 70%）

- 完成 `BASE` 正式接管主页
- 所有局外页面返回逻辑统一
- `BASE + currentBaseTab` 驱动图鉴/事件已完成
- `BaseScreen` 独立入口已完成
- 待完成：`BaseScreen` 深度拆分、`START` 过渡职责清理

### M2：事件派遣可手选（进行中，约 88%）

- 完成区域卡片 + 选择弹层
- 完成当前选择展示、清除、自动推荐
- 发起派遣不再隐式自动选已完成
- 待完成：可选组件拆分与结构整理

### M3：第一批持有物战斗化（已完成）

- 至少完成 `leftovers / quick_claw / scope_lens / bright_powder / kings_rock`
- 上述 5 个物品已接入战斗钩子
- 第一组扩展（`shell-bell + sitrus/lum/cheri/chesto/pecha/rawst/persim`）已接入
- 安全扩展（`lax-incense + 类型增伤道具组`）已接入
- 安全扩展（`liechi/petaya/salac`）已接入
- 参考清单剩余道具（`choice_band/focus_band/white_herb/mental_herb/leppa_berry/thick_club/leek/deep_sea_scale`）已接入
- 待补专项平衡测试与表现文案整理（工程收尾）

### M4：状态型持有物与恢复兼容（进行中，约 85%）

- 完成 `white_herb / focus_band / mental_herb / choice_band`
- 已完成：上述 4 个道具核心效果均已落地，`choice_band` 锁招逻辑已接入
- 待完成：恢复链路压力测试与边界行为回归

## 七、建议的实际开发顺序

如果按“一次一个模块稳定推进”的方式，建议这样排（已按当前进度重排）：

1. `BaseScreen` 拆分与 `START` 职责收敛（完成 M1 收尾）
2. 事件派遣 UI 组件拆分（可维护性优化，完成 M2 收尾）
3. 持有物配置层（`factoryHeldItems.ts` / `heldItemResolution.ts`）
4. 第一批持有物接入战斗钩子
5. 一次性持有物状态 + battle resume
6. `choice_band` 与锁招逻辑

## 八、额外建议

- 这 3 项里，只有持有物属于“规则层改造”，风险最高
- `BASE` 和事件派遣更偏 UI + 状态机，适合作为先手任务
- 等 `BASE` 独立后，再做商店、培育、个人档案会顺很多
