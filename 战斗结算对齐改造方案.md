# 战斗结算对齐改造方案

更新日期：2026-04-15

## 目标

- 以 `reference/pokeemerald-expansion` 为行为基准，逐步把当前前端内置的简化战斗逻辑升级为“可验证的、可扩展的、接近原作”的结算系统。
- 先解决“伤害、命中、属性、状态、回合末、多段攻击、副效果”这些直接影响对战真实性的核心问题。
- 在不一次性推翻现有 UI/流程的前提下，把当前单文件大函数拆成稳定的数据层、判定层、结算层。

## 非目标

- 本轮不追求一步到位覆盖 `pokeemerald-expansion` 的全部双打、联机、录像、脚本战斗表现。
- 本轮不优先做动画表现同步，先保证数值、状态、时序正确。
- 本轮不直接搬运 C 脚本式战斗引擎，而是构建一个适合当前 TypeScript 项目的“等价行为模型”。

## 当前实现的关键限制

### 1. 状态模型过于简化

- 当前 `GamePokemon` 只有一个 `status?: string` 字段。
- 这导致非挥发状态、挥发状态、限制类状态被混在一起，例如 `sleep`、`confusion`、`taunt`、`encore` 都可能被塞进同一个字段。
- 缺少睡眠回合数、剧毒计数、混乱回合数、挑衅/再来一次/定身法等独立计时器。

受影响文件：

- `src/types.ts`
- `src/features/game/hooks/useBattleController.ts`
- `src/features/game/lib/battleResolution.ts`
- `src/services/saveManager.ts`

### 2. 招式数据不足以支撑完整结算

- 当前 `Move` 结构只保留了 `power`、`accuracy`、`ailmentChance`、`flinchChance`、`statChanges`、`drain`、`healing`、`critRate` 等少数字段。
- 缺少决定实际行为的核心信息，例如：
  - 招式效果 ID
  - 多段攻击规则
  - 优先级
  - 是否接触
  - 是否受 Protect 影响
  - 是否可被 Substitute 挡住
  - 是否音波/拳类/粉末/弹道
  - 额外副效果列表和触发条件
- 仅依赖 PokeAPI `meta` 不足以恢复参考项目中的完整行为。

受影响文件：

- `src/types.ts`
- `src/services/pokeApi.ts`
- 未来新增 `src/features/game/data/battle/*.ts`

### 3. 结算流程集中在单个 Hook 中

- `useBattleController.ts` 同时负责：
  - 出手前检查
  - 命中
  - 伤害
  - 道具触发
  - 副效果
  - 天气/场地
  - 回合末
  - AI 决策支撑
- 当前文件已经承担过多职责，后续如果继续往里堆规则，回归风险会非常高。

### 4. 参考项目和当前项目的数据语义不一致

- 参考项目使用：
  - 非挥发状态 `status1`
  - 挥发状态 `status2 / volatiles / protect structs / side statuses / field statuses`
  - 招式效果枚举
  - 物品效果枚举
  - 能力效果枚举
- 当前项目使用自由字符串和少量布尔值，很难直接映射参考行为。

## 目标架构

## 一、数据分层

### 1. 静态物种层

保留当前 `Pokemon` / `Move` 的基础资料职责，但不要再让它们直接承担“战斗运行态”。

建议新增：

- `SpeciesBattleData`
- `MoveBattleData`
- `AbilityBattleData`
- `ItemBattleData`

其中 `MoveBattleData` 至少需要补齐：

- `effectId`
- `priority`
- `flags`
- `target`
- `critStage`
- `drainPercent`
- `recoilPercent`
- `healingPercent`
- `strikeMode`
- `minHits`
- `maxHits`
- `guaranteedHits`
- `secondaryEffects`
- `statusEffect`
- `statStageEffects`
- `makesContact`
- `soundMove`
- `powderMove`
- `ballisticMove`
- `bypassProtect`
- `ignoreAccuracyCheck`

### 2. 战斗运行态层

建议从 `GamePokemon` 中拆出战斗专用运行态：

- `BattlePokemonState`
- `BattleSideState`
- `BattleFieldState`
- `BattleTurnState`

`BattlePokemonState` 建议至少包含：

- `speciesId`
- `currentHp`
- `maxHp`
- `currentTypes`
- `baseAbility`
- `effectiveAbility`
- `heldItemId`
- `statStages`
- `ppState`
- `nonVolatileStatus`
- `nonVolatileStatusState`
- `volatileStates`
- `turnFlags`
- `lastMoveState`
- `gimmickState`

### 3. 战斗上下文层

新增纯计算上下文，避免直接在 React state 上边算边写：

- `BattleContext`
- `MoveResolutionContext`
- `DamageContext`
- `AccuracyContext`
- `EndTurnContext`

这些对象只服务单次结算，不直接持久化。

## 二、模块边界

建议把当前 `useBattleController.ts` 中的规则下沉到独立模块：

- `src/features/game/battle/engine/createBattleSnapshot.ts`
- `src/features/game/battle/engine/resolveTurnOrder.ts`
- `src/features/game/battle/engine/resolveMoveUse.ts`
- `src/features/game/battle/engine/resolveAccuracy.ts`
- `src/features/game/battle/engine/resolveDamage.ts`
- `src/features/game/battle/engine/resolveTypeEffectiveness.ts`
- `src/features/game/battle/engine/resolveMoveEffects.ts`
- `src/features/game/battle/engine/resolveStatusChecks.ts`
- `src/features/game/battle/engine/resolveEndTurn.ts`
- `src/features/game/battle/engine/resolveSwitch.ts`
- `src/features/game/battle/engine/resolveItems.ts`
- `src/features/game/battle/engine/resolveAbilities.ts`
- `src/features/game/battle/engine/emitBattleMessages.ts`

React Hook 最终只负责：

- 收集当前 UI 输入
- 调用结算引擎
- 把返回的快照和消息写回状态
- 驱动动画和菜单切换

## 三、参考项目对齐策略

### 1. 把参考项目当作“行为真值”，不是直接移植代码

核心参考文件：

- `reference/pokeemerald-expansion/src/battle_util.c`
- `reference/pokeemerald-expansion/src/battle_move_resolution.c`
- `reference/pokeemerald-expansion/src/battle_end_turn.c`
- `reference/pokeemerald-expansion/src/battle_script_commands.c`
- `reference/pokeemerald-expansion/src/battle_main.c`

对齐方式：

- 先对齐规则入口和数据语义。
- 再对齐每个子系统的判定顺序。
- 最后补充特殊招式和边缘条件。

### 2. 建立“本地规则表 + 参考提取脚本”

PokeAPI 无法完整提供参考项目所需字段，因此建议新增：

- `scripts/extract-battle-move-overrides.*`
- `scripts/extract-battle-item-effects.*`
- `scripts/extract-battle-ability-effects.*`

输出到：

- `src/features/game/data/battle/moveEffectTable.ts`
- `src/features/game/data/battle/moveFlags.ts`
- `src/features/game/data/battle/itemEffects.ts`
- `src/features/game/data/battle/abilityEffects.ts`

原则：

- 能从 PokeAPI 稳定拿到的，继续保留。
- PokeAPI 缺失但参考项目有定义的，用本地覆盖表补齐。
- 需要复杂行为的，不在 `Move` 上堆自由字段，而是统一走 `effectId + effect params + flags`。

## 分阶段实施

## Phase 0：建立基线与可回归验证

目标：

- 在重构前把当前战斗行为固定下来，避免改造过程中出现“修一个坏三个”但无法定位。

执行项：

- 新增战斗回归测试入口：
  - `src/features/game/battle/__tests__/damage.spec.ts`
  - `src/features/game/battle/__tests__/status.spec.ts`
  - `src/features/game/battle/__tests__/turn-order.spec.ts`
- 先写一批“当前应失败的对齐用例”，标记为 `todo` 或 `skip`。
- 用参考项目整理第一批黄金样例：
  - STAB
  - 双属性克制
  - 暴击
  - 剧毒递增
  - 睡眠回合
  - 多段攻击
  - Protect

验收：

- 项目内有独立战斗测试目录。
- 能直接运行并看到哪些行为尚未对齐。

## Phase 1：重做状态数据结构

目标：

- 把“状态字符串”改造成能表达参考项目规则的结构。

执行项：

- 在 `src/types.ts` 新增：
  - `NonVolatileStatus`
  - `NonVolatileStatusState`
  - `VolatileStateMap`
  - `BattlePokemonState`
- 睡眠、冰冻、麻痹、灼伤、中毒、剧毒分离。
- 混乱、畏缩、挑衅、再来一次、定身法、迷人、诅咒、梦魇、保护等作为挥发状态独立存储。
- `Mental Herb` 不再操作总 `status` 字段，而是清理对应挥发状态。
- 更新存档迁移逻辑，兼容旧存档：
  - 旧 `status` 自动迁移到新结构
  - 无法识别的旧值进入降级清理路径

涉及文件：

- `src/types.ts`
- `src/services/saveManager.ts`
- `src/features/game/hooks/usePokeFactoryGame.ts`
- `src/features/game/hooks/useBattleController.ts`

验收：

- 运行态已经能区分：
  - 非挥发状态
  - 挥发状态
  - 每种状态的剩余回合/计数

## Phase 2：补齐招式、道具、能力数据

目标：

- 让引擎有足够的信息决定“一个招式到底怎么结算”。

执行项：

- 扩展 `Move` 或引入新类型 `MoveBattleData`。
- 在 `src/services/pokeApi.ts` 保留基础抓取，但不要继续把所有行为压成少量字段。
- 新增本地覆盖表映射参考项目中的：
  - move effect
  - strike count
  - move flags
  - protect interaction
  - substitute interaction
  - ability hooks
  - item hooks
- 先补齐当前工厂租借池里实际会出现的招式、道具、能力；结构按全量设计，数据可分批补。

验收：

- 任意一个招式都能被统一解析为：
  - 基础威力/命中/类别
  - 效果 ID
  - flag 集合
  - 副效果列表
  - 多段攻击规则

## Phase 3：拆分结算流水线

目标：

- 从 React Hook 中抽出纯函数式战斗引擎。

建议流水线：

1. `resolveActionSelection`
2. `resolveBeforeMoveChecks`
3. `resolveAccuracy`
4. `resolveDamage`
5. `resolvePrimaryMoveEffect`
6. `resolveSecondaryEffects`
7. `resolveOnHitItemsAbilities`
8. `resolveFaintAndReplacement`
9. `resolveEndTurn`

执行项：

- 先保留现有 Hook API，不动 UI。
- `useBattleController.ts` 改成调用新引擎并消费结果。
- 引擎返回：
  - 新快照
  - battle log events
  - hp change events
  - faint events
  - animation hints

验收：

- `calculateDamage`、`resolvePreTurnStatus`、`applyMoveSecondaryEffects` 不再散落在 Hook 中。
- 新结算模块可以脱离 React 单测。

## Phase 4：对齐命中、属性、伤害

目标：

- 先把用户最容易感知的数值结果做对。

必须补齐：

- STAB
- Tera STAB
- 双属性克制
- 命中/闪避等级
- 晴/雨伤害修正
- 暴击等级与阻断
- 烧伤物攻减半
- 基础随机浮动
- 物理/特殊对应能力、道具修正
- Protect 减伤与无效
- Substitute 对部分招式的阻挡

后续增量补齐：

- 能力影响命中
- 能力影响免疫/吸收/增伤
- 物品影响命中/伤害
- 场地影响威力
- 多目标减伤

参考映射：

- `GetSameTypeAttackBonusModifier`
- `CalcTypeEffectivenessMultiplier`
- `GetTotalAccuracy`
- `CalcCritChanceStage`
- `CalculateBaseDamage`

验收：

- 伤害样例能和参考项目在允许随机浮动范围内一致。
- 基础命中判定与常见免疫/保护判定一致。

## Phase 5：对齐状态与回合末

目标：

- 把状态、计时、回合末伤害恢复做成统一时序。

必须补齐：

- 睡眠计时递减，不再使用固定概率苏醒
- 冰冻解冻规则
- 麻痹不能行动
- 混乱回合与自伤
- 普通中毒和剧毒分离
- 剧毒计数增长
- 灼伤回合末伤害
- 天气回合末伤害
- 剩饭、树果、Poison Heal 等回合末或阈值触发
- `Uproar`、`Nightmare`、`Curse` 等状态联动

执行方式：

- 把回合末改成统一的 end-turn pass，不在单次攻击结算里临时扣“毒/烧伤”。
- `battleResolution.ts` 现有函数可以保留为过渡层，但最终应并入新的 `resolveEndTurn.ts`。

验收：

- 目标在一次行动后不会立即错误吃到毒/烧伤伤害。
- 回合末双方状态依固定顺序统一处理。

## Phase 6：对齐多段攻击与副效果

目标：

- 让“技能攻击次数、命中率、特殊效果”从简化版升级到参考项目同类机制。

必须补齐：

- 固定多段
- 随机 2-5 段
- `Skill Link`
- `Loaded Dice`
- `Triple Kick`
- `Population Bomb`
- 多段攻击中断条件
- 每段命中/最终汇总显示
- 多段中的道具与副效果触发顺序

同时补齐：

- 主效果和附加效果的区分
- 概率异常
- 概率降能力
- 概率畏缩
- 仅命中后触发的效果
- 受 Protect/Substitute 影响的副效果

验收：

- 多段攻击不再按单次伤害模拟。
- 不同多段招式能按自身规则决定命中次数和流程。

## Phase 7：补齐保护、替身、切换与限制状态

目标：

- 让战斗时序从“能打出来”升级到“行为边界正确”。

必须补齐：

- Protect / Detect / King's Shield / Spiky Shield
- Substitute
- Taunt
- Encore
- Disable
- Torment
- Choice item 锁招
- 强制切换/主动替换后的状态清理
- Baton Pass 保留规则可留到本阶段后半段

验收：

- 保护类招式不再只是名字识别。
- `Mental Herb` 的效果变成对挥发限制状态生效，而不是清空总状态。

## Phase 8：同步 AI 与评分逻辑

目标：

- 避免引擎变复杂后，AI 仍基于旧的简化假设选招。

执行项：

- AI 伤害估算改为调用新引擎的“静态估算接口”。
- AI 识别：
  - 保护
  - 多段
  - 状态免疫
  - Tera 防守/进攻收益
  - 锁招风险
- `estimateEnemyMoveScore` 使用新 `MoveBattleData`。

验收：

- AI 不会继续把“无效招式”或“被限制招式”打出高分。

## Phase 9：存档兼容与切换上线

目标：

- 平滑替换当前对战存档结构，避免已有局内战斗恢复失效。

执行项：

- `SAVE_SCHEMA_VERSION` 升级。
- 为旧战斗存档提供迁移器。
- 对旧快照中不存在的新字段给出默认值。
- 若迁移失败，允许安全回退为“无法恢复该战斗，但不损坏主存档”。

验收：

- 新旧存档导入有明确结果。
- 不会因为状态结构升级导致应用直接崩溃。

## 推荐实施顺序

建议按下面的提交粒度推进，而不是一次性大改：

1. Phase 0 + Phase 1
2. Phase 2
3. Phase 3
4. Phase 4
5. Phase 5
6. Phase 6
7. Phase 7
8. Phase 8 + Phase 9

原因：

- 先稳定数据结构，再补规则，能减少反复返工。
- 先拆引擎再补行为，测试才有落点。
- 先解决伤害/状态，再做保护和复杂招式，收益最高。

## 第一批建议直接开工的改动

如果按最短路径进入实施，建议下一步就做这几件事：

1. 在 `src/types.ts` 引入新的状态模型，不立刻删旧字段，只做兼容并行。
2. 新建 `src/features/game/battle/engine/` 目录，把 `calculateDamage` 和 `resolvePreTurnStatus` 先迁出去。
3. 在 `src/services/pokeApi.ts` 扩展 `Move` 结构，先增加：
   - `priority`
   - `effectId`
   - `minHits`
   - `maxHits`
   - `flags`
   - `secondaryEffects`
4. 新增一个参考覆盖表文件，先覆盖工厂租借池内常见招式。
5. 写第一批对齐测试：
   - STAB
   - 剧毒递增
   - 睡眠回合
   - Skill Link 五连击
   - Protect 阻挡

## 风险与应对

### 风险 1：PokeAPI 数据不够

应对：

- 保留 PokeAPI 作为基础来源。
- 引入本地覆盖表和参考提取脚本。

### 风险 2：Hook 重构期间 UI 易受影响

应对：

- 先让 Hook 调新纯函数，不动组件接口。
- 每阶段保留兼容适配层。

### 风险 3：存档结构升级导致恢复失败

应对：

- 先实现迁移器。
- 迁移失败时安全降级，不破坏非战斗存档。

### 风险 4：一次性覆盖面过大

应对：

- 先对齐最影响体验的机制。
- 对未覆盖招式临时走“旧逻辑 fallback”，但明确记录白名单。

## 完成标准

当以下条件满足时，可认为“核心战斗结算已完成对齐改造”：

- 常规单打伤害与参考项目主要样例一致。
- STAB、属性克制、命中、暴击、天气、场地、常见道具修正正确。
- 睡眠、冰冻、麻痹、灼伤、中毒、剧毒、混乱、畏缩具备正确时序和计时。
- 多段攻击、Protect、Substitute、Choice 锁招、Taunt/Encore/Disable 可正常工作。
- 战斗结算已从 React Hook 中下沉为可测试引擎。
- 存档迁移完成，旧版本数据可安全处理。

## 本轮执行建议

本方案落地后，下一轮建议直接进入：

- “Phase 1：状态模型改造”
- 同时补上 “Phase 0：战斗基线测试”

这是当前改造的最稳起点，也是后面所有规则对齐的基础。
