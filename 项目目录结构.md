# pokeFactory2 项目现状与目录说明

> 生成时间：2026-04-03（本地）  
> 项目路径：`D:\Games\pokeFactory2`

## 一、当前项目情况

- 技术栈：`React 19` + `TypeScript` + `Vite`。
- 运行命令：`npm run dev`（端口 `3000`）。
- 构建命令：`npm run build`。
- 类型检查：`npm run lint`（实际执行 `tsc --noEmit`）。
- Git 状态：当前分支 `main`，存在未提交改动：
  - `src/App.tsx`（已修改）
  - `src/features/`（新增目录）
- 需注意：`src/App.tsx` 中包含 Git 冲突标记（如 `<<<<<<< ours` / `>>>>>>> theirs`），建议尽快完成冲突清理后再继续开发或发布。

## 二、项目目录（已过滤 `.git` / `node_modules` / `dist`）

```text
pokeFactory2/
├─ .env.example                    # 环境变量示例（本地开发可复制为 .env.local）
├─ .gitignore                      # Git 忽略规则
├─ index.html                      # Vite 入口 HTML 模板
├─ metadata.json                   # AI Studio 相关元数据
├─ package.json                    # 项目依赖与 npm 脚本配置
├─ README.md                       # 项目基础说明（运行与部署）
├─ tsconfig.json                   # TypeScript 编译配置
├─ vite.config.ts                  # Vite 构建与开发服务器配置
└─ src/
   ├─ App.tsx                      # 应用根组件（当前处于重构/合并冲突状态）
   ├─ main.tsx                     # React 挂载入口
   ├─ index.css                    # 全局样式
   ├─ constants.ts                 # 通用常量（如规则、配置等）
   ├─ types.ts                     # 全局类型定义
   ├─ uiAppConstants.ts            # UI 显示常量（图标/文案映射等）
   ├─ uiStrings.ts                 # 多语言文案资源
   ├─ components/
   │  └─ TypeBadge.tsx             # 属性标签等通用展示组件
   ├─ services/
   │  └─ pokeApi.ts                # 宝可梦相关 API 请求与数据处理
   └─ features/
      └─ game/                     # 核心玩法模块（按功能拆分）
         ├─ view-model.ts          # 游戏视图模型类型/契约
         ├─ components/            # 游戏界面组件层
         │  ├─ GameView.tsx        # 游戏主视图容器
         │  ├─ LoadingScreen.tsx   # 启动与加载状态界面
         │  └─ game-view/
         │     ├─ shared.ts        # game-view 子模块共享工具/类型
         │     ├─ StartScreen.tsx  # 开始界面（模式/参数选择）
         │     ├─ FactorySelectScreen.tsx  # 对战工厂初始选择界面
         │     ├─ BattleScreen.tsx          # 战斗主界面
         │     ├─ BattleLogHistory.tsx      # 战斗日志历史面板
         │     ├─ FactorySwapScreen.tsx     # 工厂交换选择界面
         │     ├─ RewardScreen.tsx          # 奖励结算界面
         │     ├─ PokemonInfoScreen.tsx     # 精灵详情面板
         │     ├─ GameHeader.tsx            # 顶部状态栏
         │     ├─ GameOverScreen.tsx        # 失败/结束界面
         │     ├─ TransitionOverlay.tsx     # 场景切换过渡层
         │     ├─ battle/                   # 战斗 UI 子组件
         │     │  ├─ BattleActionPanel.tsx
         │     │  ├─ BattleBagPanel.tsx
         │     │  ├─ BattleFieldPanel.tsx
         │     │  ├─ BattleLogPanel.tsx
         │     │  ├─ BattleMainMenu.tsx
         │     │  ├─ BattleMoveEffect.tsx
         │     │  ├─ BattleMovesPanel.tsx
         │     │  ├─ BattlePokemonPanel.tsx
         │     │  ├─ CatchEffectOverlay.tsx
         │     │  ├─ EnemyBattleCard.tsx
         │     │  └─ PlayerBattleCard.tsx
         │     └─ reward/                   # 奖励与替换流程组件
         │        ├─ RewardCard.tsx
         │        ├─ RewardCardsGrid.tsx
         │        ├─ RewardHeader.tsx
         │        ├─ ReplacePokemonModal.tsx
         │        ├─ LearnMoveOverlay.tsx
         │        └─ EvolutionOverlay.tsx
         ├─ hooks/                 # 玩法逻辑 hooks（状态与流程编排）
         │  ├─ usePokeFactoryGame.ts   # 总入口 hook，聚合各流程
         │  ├─ useBattleController.ts  # 战斗回合与动作控制
         │  ├─ useFactoryFlow.ts       # 工厂流程（开局/选租借/切关）
         │  ├─ useRewardFlow.ts        # 奖励流程（重抽/替换/进化）
         │  └─ useGameLocalization.ts  # 本地化文本与显示处理
         ├─ lib/
         │  └─ battleResolution.ts     # 战斗结算/伤害计算逻辑
         └─ view-model/            # view-model 拆分实现
            ├─ actions.ts          # 行为动作定义
            ├─ common.ts           # 通用逻辑与共享方法
            ├─ setters.ts          # 状态写入函数
            └─ state.ts            # 状态结构定义
```

## 三、维护建议（简版）

- 第一优先级：先处理 `src/App.tsx` 冲突标记，确保可编译。
- 第二优先级：继续将历史“巨型组件”逻辑收敛到 `features/game` 模块中，保持 hooks + view-model 的分层。
- 第三优先级：补充最小化自动检查（例如对关键 battle/reward 流程做单测或集成 smoke test）。
