# 对战工厂数据移植清单（对比参考项目）

## 目标
将参考项目 Battle Factory 的“数据层”与本项目对齐，明确当前缺口与迁移优先级。

## 当前已对齐（机制层）
1. 租借池与对手池共用同一生成器（同源逻辑）。
2. 租借历史 rank（15/22/29/36/43）影响租借池质量。
3. 按挑战进度分档（challenge tier）进行池质量提升。
4. 种族不重复、道具不重复、对手避开租借池/玩家队伍种族。

## 待移植数据（按优先级）

### P0（先做）
1. Frontier 固定宝可梦池（静态 sets）
- 参考：`gBattleFrontierMons`（species/moves/heldItem/ev/nature/ball）
- 现状：运行时从 PokeAPI 随机拉取，非固定 set。
- 影响：无法实现“同档可复现”的工厂体验。
- 进展（增强版）：
  - 已扩展 `factoryReferenceSets.ts` 到 `frontierMonId 110..881`（772 sets），覆盖 Lv50 与 Open 区间。
  - 已新增可复用生成脚本：`scripts/generate_factory_reference_sets.cjs`。

2. FrontierMon 索引与档位边界
- 参考：`FRONTIER_MON_*` 常量与高档边界（如 `FRONTIER_MONS_HIGH_TIER`）。
- 现状：仅用质量偏置模拟，无真实索引边界表。

3. 区间抽样表
- 参考：`sInitialRentalMonRanges`（Lv50/Open, challenge 区间）。
- 现状：质量偏置近似，尚未移植原始表驱动。

### P1（第二阶段）
4. 训练家库与组队模板
- 参考：`gBattleFrontierTrainers` + `FRONTIER_MONS_*` 分组。
- 现状：敌人非训练家模板驱动，缺“训练家-队伍组”结构。
- 进展（MVP 已接入）：
  - 已从参考项目解析并生成 `factoryTrainerTemplates.ts`（299 个训练家模板，含 `facilityClass + monSet`）。
  - 敌方生成已改为“先选训练家模板 -> 再按模板参数组队”（模板控制质量偏置与 better-range 槽位）。
  - 已实现“同一组（setNo）内尽量不重复训练家”。
  - 当前仍是“模板参数驱动”的最小实现，尚未做到与原作 `FRONTIER_MONS_*` 精确一一映射抽样。
- 进展（第 2 层已接入）：
  - 已补齐到带参数宏的 `monSet` 表达式解析（如 `FRONTIER_MONS_PSYCHIC_2(...)`、`..._EXTRA(...)`）。
  - 新增 `factoryTrainerMonSetPools.ts`，将训练家模板的 `monSet` 显式映射为 `frontierMonId` 池（来自 `battle_frontier_trainer_mons.h`）。
  - 敌方组队时已接入 `monSet` 池过滤：若当前参考子集存在交集，则优先在该交集内抽样；无交集时回退到现有参考池/随机兜底。
  - 说明：当前项目参考子集仍为 `frontierMonId 110..371`，高段训练家的 `monSet` 常见在更高编号区间，因此会触发部分回退路径，这是预期行为。

5. 固定 IV 档位表与首领特例
- 参考：`sFixedIVTable`、`GetFactoryMonFixedIV`、Factory Brain 特例。
- 现状：随机 IV + 项目自定义 Boss 增益。

6. 禁用/过滤物种数据
- 参考：Unown 禁用 + Frontier banned 生态。
- 现状：仅基础过滤。

### P2（增强）
7. 战术风格数据
- 参考：`sRequiredMoveCounts` 与 move effect->style。
- 现状：尚未移植。

8. 完整挑战状态持久化字段
- 参考：winStreak flags/masks/trainerIds 等。
- 现状：核心状态已可用，细节未全量对齐。

## 执行顺序建议
1. 先落地 P0-1：引入“固定 Frontier 子集池”（最小可用）。
2. 再补 P0-2/P0-3：显式索引与区间表数据化。
3. 之后扩展 P1：训练家模板 + IV 表。
4. 最后做 P2：风格与完整持久化细节。
