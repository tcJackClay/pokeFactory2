# 对战特殊形态触发规则（项目实现说明）

## 本次已落地

1. 已补充“额外形态池”配置文件：`src/features/game/config/specialForms.ts`。
2. 已把随机抽取从“仅数字图鉴号”升级为“数字图鉴号 + 形态名（PokeAPI）”混合抽取。
3. 已在工厂租借池与奖励池接入形态抽取，并加入失败自动跳过重试，避免个别形态名不可用导致流程中断。
4. 已自动生成参考项目额外形态清单（1026~1572）与规则分类：
   - `src/features/game/config/referenceExtraSpecies.ts`
   - `reference/pokeemerald-expansion/SPECIAL_SPECIES_RULES_ZH.md`

## 特殊形态触发规则（用于玩法设计）

1. `DIRECT`：可直接作为普通宝可梦出场。
2. `HOLD_ITEM`：需要携带特定道具才能维持/切换形态。
3. `BATTLE_MEGA`：Mega 进化，必须在战斗中触发，通常需要对应 Mega 石与钥石。
4. `BATTLE_DYNAMAX_GMAX`：极巨/超极巨，必须在极巨化战斗流程中触发。
5. `BATTLE_TERA`：太晶化/星晶化，必须在太晶化战斗流程中触发。
6. `BATTLE_ABILITY`：由特性/天气/回合状态触发（如鱼群、画皮破裂等），不应直接作为初始形态发放。
7. `EVENT_ONLY`：剧情/特殊流程形态（合体、剧情霸主等），不建议直接放入工厂常规池。

## 当前工厂模式建议

1. 工厂租借池优先使用 `DIRECT` 与部分 `HOLD_ITEM`。
2. `BATTLE_MEGA / BATTLE_DYNAMAX_GMAX / BATTLE_TERA / BATTLE_ABILITY / EVENT_ONLY` 暂不直接投放，待对应战斗系统完成后再开启。
3. 后续若接入 Mega/极巨/太晶流程，可按 `referenceExtraSpecies.ts` 的分类逐步放开。
