# 宝可梦对战工厂恢复评估与迭代计划

> 项目：`D:\Games\pokeFactory2`  
> 日期：2026-04-03

## 1. 当前实现现状（已具备）

- 已有对战工厂基础循环：
  - 开局 6 选 3（`FACTORY_SELECT`）
  - 进入 3v3 对战（`BATTLE`）
  - 胜利后可换怪（`FACTORY_SWAP`）
  - 下一关继续
- 战斗系统具备基础能力：
  - 属性克制、命中、暴击、异常状态、能力阶段、天气伤害框架
  - 背包道具使用、换宠、捕捉动画层
- 架构已模块化：
  - `useFactoryFlow`（工厂流程）
  - `useBattleController`（战斗控制）
  - `useRewardFlow`（奖励流程）
  - `GameView` + 分屏组件

## 2. 关键问题（影响“还原对战工厂”）

### P0（优先马上修）

1. 奖励流程未接入主循环
- 现状：`REWARD` 页面与 `useRewardFlow` 逻辑存在，但胜利后直接进 `FACTORY_SWAP`，未进入奖励阶段。
- 代码：`src/features/game/hooks/useBattleController.ts:236` 直接 `setGameState('FACTORY_SWAP')`。
- 影响：奖励系统形同“半断路”，金币/商店循环不成立。

2. 金币经济未闭环
- 现状：金币只有扣减逻辑（重抽、商店购买），没有稳定获取入口。
- 代码：`useRewardFlow.ts:164`、`:187` 扣金币；工厂开局 `setCoins(0)` 在 `useFactoryFlow.ts:165`。
- 影响：商店与 reroll 基本不可用。

3. 工厂规则与官方模式不一致（系统混搭）
- 现状：工厂流程里混入捕捉、道具商店、随机奖励等 roguelike 机制。
- 影响：如果目标是“还原对战工厂”，当前规则偏离较大，需要模式收敛。

### P1（建议第二阶段）

4. 天气状态变量未真正驱动
- 现状：`weather` 在伤害结算里被使用，但没有完整的设置/回合管理流程。
- 代码：`usePokeFactoryGame.ts` 中 `weatherTurns` 被 `void` 掉。
- 影响：天气属于“半实现”。

5. 敌方替补查找存在同种宝可梦边界风险
- 现状：`sendOutNextEnemy` 通过 `excludeId` 排除已倒下目标，若敌队有重复 `id`，可能误判无可上场成员。
- 代码：`findNextLivingLeadIndex(..., { excludeId })`。
- 影响：小概率导致提前判胜。

6. 本地化文本质量不稳定
- 现状：部分中文显示可能受编码/文案一致性影响（名称与术语混用）。
- 影响：体验层不够“工厂风味”，且不利于长期维护。

## 3. 可加入的新功能（按收益排序）

1. `经典工厂模式` 与 `Rogue 工厂模式` 双模式切换（最推荐）
- 经典：贴近原作规则（无捕捉/无商店/以连胜和组别晋级为核心）
- Rogue：保留你现在的奖励、商店、成长玩法

2. `组别机制`（每 7 场一个组别）
- 第 7 场为馆主/首领战，结算组别连胜
- 显示“当前组别内胜场 + 总连胜”

3. `租借池质量控制`
- 按档位筛选招式组合、个体与性格，避免随机到“无效招式集”

4. `AI 选招优化`
- 从纯随机改成简单评分（击杀概率、克制、命中、收益）

5. `数据持久化`
- 本地保存最高连胜、历史组别结果、最近队伍

6. `每日挑战种子`
- 固定随机种子，让同一日玩家面对同样租借池，利于挑战分享

## 4. 建议实施计划（4 个迭代）

## 迭代 A：先把主流程打通（1-2 天）
- 明确模式目标：
  - 若“还原工厂”为主：胜利后 `BATTLE -> FACTORY_SWAP -> BATTLE`，暂时关闭奖励页入口
  - 若保留混合玩法：胜利后 `BATTLE -> REWARD -> FACTORY_SWAP/下一关`
- 修复金币来源（如每胜 + 固定金币，或仅 Rogue 模式有金币）
- 修复重复 `id` 替补边界问题
- 验收：完整跑通 3 场以上循环无卡死、无空奖励页

## 迭代 B：对战工厂规则还原（2-3 天）
- 增加“组别(7战) + 首领战”
- 统一每战后恢复逻辑（HP/异常/能力阶段重置策略）
- 限制非工厂机制（经典模式关闭捕捉和商店）
- 验收：经典模式连续打完 7 战，规则与预期一致

## 迭代 C：增强可玩性（2-4 天）
- AI 选招评分
- 租借池质量控制（招式、性格、个体）
- UI 信息补全（组别进度、连胜结算）
- 验收：对局策略性明显提升，低效局减少

## 迭代 D：质量与发布（1-2 天）
- 文案与本地化统一（中/英）
- 关键流程测试（开局、选租借、战斗、换怪、结算）
- 性能与稳定性检查
- 验收：可稳定连续运行多轮

## 5. 我建议的执行顺序（最务实）

1. 先做“模式定稿”（经典工厂 or 双模式）
2. 立刻修主流程与金币闭环（迭代 A）
3. 再做组别与首领战（迭代 B）
4. 最后打磨 AI 与租借池质量（迭代 C）

---

## 附：本次检查涉及的核心文件

- `src/features/game/hooks/useBattleController.ts`
- `src/features/game/hooks/useFactoryFlow.ts`
- `src/features/game/hooks/useRewardFlow.ts`
- `src/features/game/components/GameView.tsx`
- `src/features/game/components/game-view/FactorySelectScreen.tsx`
- `src/features/game/components/game-view/FactorySwapScreen.tsx`
- `src/features/game/lib/battleResolution.ts`
